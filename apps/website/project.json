{
    "root": "apps/website",
    "targets": {
      "test": {
        "executor": "@nx/jest:jest",
        "options": {}
      },
      "build": {
        "executor": "nx:run-commands",
        "options": {
          "cwd": "apps/website",
          "command": "astro check && dotenv -- astro build"
        },
        "configurations":{ 
          "staging":{
            "command": "astro check && tsc --noEmit && dotenv -e ../../.env.staging -- astro build"
          },
          "production":{
            "command": "astro check && tsc --noEmit && dotenv -e ../../.env.production -- astro build"
          }
        }
      },
      "deploy": {
        "executor": "nx:run-commands",
        "options": {
          "cwd": "apps/website",
          "command":"dotenv -- vercel deploy --prebuilt"
        }
      },
      "dev": {
        "executor": "nx:run-commands",
        "options": {
          "cwd": "apps/website",
          "command":"dotenv -e ../../.env.development -- astro dev"
        }
      },
      "preview": {
        "executor": "nx:run-commands",
        "options": {
          "cwd": "apps/website",
          "commands":[
            "astro preview"
          ]
        }
      }
    }
  }   
  